"use strict";

angular.module("frontendAngularClientApp")
    .controller("HomeController", function ($scope, $state, $rootScope, $stateParams) {
        (function () {
            //$rootScope.IsBusy = true;
            //leadsService.Get($stateParams.id).then(function (resp) {
            //    $scope.entity = resp.data;
            //    $rootScope.SetPageTitle("Lead", $scope.entity.id);
            //    $rootScope.IsBusy = false;
            //}, $rootScope.raiseErrorDelegate);
        })();        
    });